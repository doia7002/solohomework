#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

// numbers_len은 배열 numbers의 길이입니다.
double solution(int numbers[], size_t numbers_len) 
{
    let sum = 0
    for (let x=0, x<numbers_len; x++)
    {
        sum+=numbers[x]
    }
    let answer = sum/numbers_len;
    return answer;
}
