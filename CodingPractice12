#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>

// arr_len은 배열 arr의 길이입니다.
double solution(int arr[], size_t arr_len) 
{
    int sum=0;
    for(int x=0; 1<arr_len; x++)
    {
        sum+=arr[x]        
    }
    double answer=(double)sum/arr_len;
    return answer;
}
